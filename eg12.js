/*if in between + has both side 2 operants are numeric, so it performs maths. 
But one side operants is a string type then it concatinate.*/
//It only performs in + operator.

var x;
x="Bombay";
var y;
y="Bombay"+20;
console.log(y);

